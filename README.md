# 🔐 Команда " AIгентство CryptoSlon"

## 🔐 Задача №3 - SAST-анализ

### Обзор
Привет, эта инструкция по установке и запуску агента для решения задачи №3 
из хакатона. 

SAST-Агент - сложный многокомпонентный агент для анализа уязвимостей кода, который: 
- Проводит анализ кода на уязвимости с помощью SEMGREP (осуществляет поиск опасных паттернов)
- Проводит анализ кода Python с помощью BANDIT 
- Склеивает отчеты SEMGREP и BANDIT по семантическим группам
- Проводит триаж-анализ отчетов и выявляет самые главные уязвимости
- Извлекает уязвимые участки кода на основе триаж-анализа
- Генерирует исправления уязвимостей с помощью LLM
- Совершает инъекцию исправлений в исходный код с сохранением отступов (он старается)
- Поддерживает GigaChat и ChatGPT "из коробки", легко может быть расширен для использования с другими моделями

Базово агент настроен на использование `gigachat-pro/max` и работает стабильно.


### Быстрый старт

### 1. Перейдите в директорию, где будете хранить Агента
```bash
cd <директория-проекта>
```

### 2. Склонируйте к себе репозиторий
```bash
git clone https://git-ai.ctflab.local:8443/cryptoslon/task-3.git
```

### 3. Создайте изолированное виртуальное окружение Python 
```bash
python3 -m venv .
# Активируйте его если нуобходимо
source .venv/bin/activate
```

### 4. Установите зависимости
```bash
pip install -r requirements.txt
```

### 5. Настройте переменных окружения
Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

Заполните файл `.env`:
```
GIGACHAT_CREDENTIALS=ваш_ключ_gigachat # необходимо
OPENAI_API_KEY=ваш_ключ_openai # опционально
```
### 6. Установка semgrep
Для запуска пайплайна нужно установить semgrep:
```bash
pip install semgrep # работает на macOS и Linux, для Windows - см. ссылку
```
semgrep требует регистрации и активации, подробности: https://semgrep.dev/docs/getting-started/quickstart

после установки выполнить команду в командной строке и проследовать инструкциям.
```bash
semgrep login
```
После этого semgrep внутри Агента должен работать.

!!! Без Semgrep агент не сможет выполнить анализ кода!!!

## Архитектура

### Модули SAST-анализа
Система SAST-анализа состоит из нескольких модулей:

**1. Сканеры безопасности**
- `semgrep_runner.py` - интеграция с Semgrep для поиска уязвимостей по паттернам
- `bandit_runner.py` - интеграция с Bandit для анализа Python-кода на уязвимости

**2. Обработка отчетов**
- `report_merger.py` - объединение отчетов от разных сканеров (Semgrep и Bandit) в единый формат
- `report_aggregator.py` - агрегация всех отчетов в JSON с категоризацией по CWE
- `rule_mappings.json` - маппинг правил сканеров на стандартные CWE-идентификаторы

**3. Анализ и исправление**
- `triage_analyzer.py` - триаж найденных уязвимостей, определение критичности и приоритетности
- `fix_generator.py` - генерация исправлений для уязвимостей с помощью LLM
- `code_injector.py` - автоматическая инъекция фиксов в код

### Агент
Система использует интеллектуальных агентов для координации и выполнения задач:

**BaseAgent** (`agents/base_agent.py`)
- Базовый класс для всех агентов
- Поддержка памяти, инструментов и RAG (Retrieval-Augmented Generation)
- Интеграция с различными LLM-провайдерами (GigaChat, OpenAI, Google, Groq)

**SASTAgent** (`agents/sast_agent.py`)
- Специализированный агент для проведения SAST-анализа
- Управляет всем пайплайном безопасности
- Координирует работу инструментов сканирования, анализа и исправления

### Инструменты агентов
Агенты используют набор специализированных инструментов (`agents/tools/`):

**SAST-инструменты** (`sast_tools.py`)
- `SASTAnalysisTool` - проведение базового SAST-анализа (Semgrep + Bandit сканирование)
- `SASTTriageTool` - триаж и приоритизация уязвимостей с помощью LLM
- `SASTFixGenerationTool` - извлечение уязвимого кода и генерация исправлений
- `SASTCodeInjectionTool` - применение фиксов в исходный код с созданием бэкапов
- `SASTFullPipelineTool` - запуск полного пайплайна (все 4 этапа последовательно)

Каждый инструмент наследуется от `BaseTool` и предоставляет стандартизированный интерфейс для агентов.

## Запуск

```bash
# чтобы видеть логику работы Агента
python main.py -l INFO

# чтобы видеть только результаты и ошибки
python main.py -l WARNING
```
После запуска вы увидите:
```
Welcome to CryptoSlon Security Platform!

============================================================
CryptoSlon - SAST Security Agent
============================================================

Select an option:
1. Analyze code for vulnerabilities (automated)
2. Run full SAST pipeline (automated)
3. Interactive mode
4. Exit

Enter your choice (1-4): 
```

Эта простая обертка позволяет быстро запустить Агента в каком-то режиме. Что бы вы не выбрали, это все равно будет Агент,
никаких скриптов, эта функция сделана исключительно для удобства. 

Опции: 
- 1 - передает Агенту команду `"perform semgrep analysis in {directory}"`
- 2 - передает Агенту команду `"run full sast pipeline for {directory}"`
- 3 - не передает Агенту никаких команд, вы останетесь с ним наедине :)
- 4 - завершает программу

При выборе 1 или 2 для удобства стоит валидатор пути:
```
Option 1: Analyze code for vulnerabilities
----------------------------------------
Enter absolute path to directory: 
```
От вас ожидается ввод корректной директории с каталогом кода, например: 
`/Users/izelikson/python/CryptoSlon/SAST/code_for_sast/taskstate`

После этого вам Агент сообщит вам, что он собрался делать: 
```
Starting SAST Agent in automated mode...
Command: perform semgrep analysis in /Users/izelikson/python/CryptoSlon/SAST/code_for_sast/taskstate
Model: gigachat-pro
============================================================
Активирован инструмент: sast_analysis
```
и начнет сыпать логами от тех инструментов, которые он использует: 
```
2025-08-29 17:10:03,351 - semgrep_analyzer - INFO - Analysis complete. Found 40 issues.
2025-08-29 17:10:03,351 - semgrep_analyzer - INFO - Results saved to: /Users/izelikson/python/CryptoSlon/SAST/code_for_sast/reports/sast_analysis_taskstate/semgrep_report.sarif
2025-08-29 17:10:03,579 - bandit_analyzer - INFO - Analysis complete. Found 11 issues.
2025-08-29 17:10:03,579 - bandit_analyzer - INFO - Results saved to: /Users/izelikson/python/CryptoSlon/SAST/code_for_sast/reports/sast_analysis_taskstate/bandit_report.sarif
2025-08-29 17:10:03,580 - report_merger - INFO - Report files status:
```
Я не успел настроить проброс уровня логирования на все тулзы, поэтому тулзы всегда будут сыпать логами, зато жюри может
быть уверено, что все честно :) 

После того, как Агент решит, что он свою работу сделал, он выведет отчет об этом: 
```
2025-08-29 17:10:03,584 - report_aggregator - INFO - Summary:
2025-08-29 17:10:03,584 - report_aggregator - INFO -   Unique vulnerability types: 6
2025-08-29 17:10:03,584 - report_aggregator - INFO -   Total findings: 42
2025-08-29 17:10:03,584 - report_aggregator - INFO -   Severity distribution: {'HIGH': 28, 'MEDIUM': 14}
2025-08-29 17:10:03,584 - report_aggregator - INFO -   Source distribution: {'bandit': 13, 'semgrep': 42}
2025-08-29 17:10:03,584 - report_aggregator - INFO - Top 5 most frequent vulnerabilities:
2025-08-29 17:10:03,584 - report_aggregator - INFO -   1. PRIVILEGE_ESCALATION (HIGH): 21 occurrences
2025-08-29 17:10:03,584 - report_aggregator - INFO -   2. SECRETS (MEDIUM): 9 occurrences
2025-08-29 17:10:03,584 - report_aggregator - INFO -   3. SENSITIVE_DATA_EXPOSURE (MEDIUM): 5 occurrences
2025-08-29 17:10:03,584 - report_aggregator - INFO -   4. CODE_INJECTION (HIGH): 4 occurrences
2025-08-29 17:10:03,584 - report_aggregator - INFO -   5. WEAK_AUTHENTICATION (HIGH): 2 occurrences

============================================================
SAST Agent completed successfully
Full response saved to: sast_report.txt
```
и вы вернетесь в начальное меню.

При выборе `2` ситуация похожая, только логов и отчетов будет больше.

При выборе `3` - интерактивный режим - вы останетесь 1 на 1 с Агентом и он будет очень внимательно вас слушать!
```
Starting SAST Agent in interactive mode...
Model: gigachat-pro
============================================================

======================================================================
 SAST Security Agent - Interactive Mode
======================================================================

 HOW TO USE:
 -----------
 The agent understands natural language commands in English.
 Simply describe what you want to analyze or do.
.
.
.
======================================================================
 Agent ready! Type your command or 'quit' to exit.
======================================================================
```
И дальше вы можете просить его:
- Проводить базовый SAST-анализ кода
- Проводить triage-анализ отчета, полученного в ходе базового SAST-анализа
- Генерировать фиксы уязвимостей на основе triage-анализа
- Делать инъекции фиксов в код
- Проводить весь пайплайн за раз

У Агента есть полный доступ ко всем входным параметрам классов, которые выполняют эти задачи, но я не успел написать 
достаточно хороший промпт, чтобы Агент хорошо этим управлял, поэтому рекомендую просто просить его запустить какую-то
тулзу - с этим он неплохо справляется. 

Самый надежный вариант - просить Агента запустить полный пайплайн для директории. 

Агент способен пройти пайплайн путем вызова отдельных тулзов и будет выдавать подробный отчет по итогам работы каждой, 
но это не всегда хорошо срабатывает и нужно очень внимательно следить за путями до директорий и файлов. 

У Агента можно спросить, какие инструменты у него есть и зачем они нужны, да и в целом он общительный.

Примеры команд, с которыми интерактивный агент хорошо справляется: 
- Сделай semgrep анализ моего кода /absolute/path/to/code/folder
- Сделай триаж анализ этого отчета /absolute/path/to/aggregated/report #работает только на aggregated-report.json
- Запусти полный пайплайн на мой код /absolute/path/to/code/folder - лучший вариант!

В результате выполнения пайплайна будет создан каталог sast_reports с подробными отчетами о каждом проделанном шаге
и файл sast_report.txt, где будет обобщенный итог о выполненной задаче от LLM.

Иногда Агент ленится и рвется запустить полный пайплайн. 

## Пример полного пайплайна
1. Скачать каталог с кодом с сервера
```bash
scp -r admin@10.62.20.10:/home/admin/taskstate_orig /Users/izelikson/Downloads/taskstate_copy
```

2. Запустить агента и выбрать пункт 2: Run full SAST pipeline
```bash
python main.py 
```
3. Ознакомиться с результатами
- отчеты каждого этапа лежат в папке sast_reports в каталоге с main.py
- отчет о работе агента лежит в файле sast_report.txt в каталоге с main.py
- фиксы кода можно найти по декораторам `#LLM FIX` внутри кода
- для всех файлов, в которые агент вносил изменения, созданы бэкапы, папка с бэкапами лежит рядом с каталогом кода

4. (Опционально) загрузить каталог с фиксами обратно на сервер
```bash
rsync -avz \                                                                             
/Users/izelikson/Downloads/taskstate_copy/ \
admin@10.62.20.10:/home/admin/taskstate-fix/
```